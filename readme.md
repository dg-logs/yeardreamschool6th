# 1. 원하는 공유 저장소에 접근

# 2. git 초기화
git init

# 3. 변경한 파일 staged
# git add [원하는 파일]
git add .

# 상태 확인
git status

# commit 하기
git commit -m "first commit"

# commit 의 상채 확인
git log --graph --oneline --all